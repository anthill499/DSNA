import TrieNode from "./TrieNode.js";
class Trie {
  constructor() {
    this.root = null;
  }

  insert(val) {
    const newTrieNode = new TrieNode(val);
  }

  totalWordCount() {}
}

const Words = ["flower", "flow", "flight"];
